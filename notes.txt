//store objects in local storage
localStore['object'] = JSON.stringify(object)
JSON.parse(localStorage['object'])

//scroll window
window.scroll(left,top);

//hide html scroll bars, css
html {
overflow:hidden;
}

To pass an Array by value instead of by reference, use the Array.slice() method.

//loops through object
for (var propertyName in theObject) {
// Check if it’s NOT a function
if (!(theObject[propertyName] instanceof Function)) {
properties += propertyName + “, “;
}
}